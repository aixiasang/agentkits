# Copy this file to `.env` and fill in whichever providers you need.

# DeepSeek (used by the default DeepSeek integration tests and all examples).
DS_API_KEY=

# Qwen / DashScope (optional; OpenAI-compatible endpoint at
#   https://dashscope.aliyuncs.com/compatible-mode/v1)
ALI_API_KEY=
